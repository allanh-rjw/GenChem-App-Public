import { drawMolecule3D, drawProton } from './molecule-3d.js';
const NET_IONIC={'strong-acid':{reactants:['Hplus','OH'],products:['H2O'],equation:'H⁺ + OH⁻ → H₂O',donor:0,acceptor:1},'weak-acid':{reactants:['HF','OH'],products:['F','H2O'],equation:'HF + OH⁻ → F⁻ + H₂O',donor:0,acceptor:1},'weak-base':{reactants:['NH3','Hplus'],products:['NH4'],equation:'NH₃ + H⁺ → NH₄⁺',donor:1,acceptor:0}};
const EQUIVALENCE_EQUILIBRIA={'weak-acid':{equation:'F⁻ + H₂O ⇌ HF + OH⁻',forward:{reactants:['F','H2O'],products:['HF','OH'],donor:1,acceptor:0,name:'F⁻ hydrolysis'},reverse:{reactants:['HF','OH'],products:['F','H2O'],donor:0,acceptor:1,name:'reverse proton transfer'}},'weak-base':{equation:'NH₄⁺ + H₂O ⇌ NH₃ + H₃O⁺',forward:{reactants:['NH4','H2O'],products:['NH3','H3O'],donor:0,acceptor:1,name:'NH₄⁺ acid reaction'},reverse:{reactants:['NH3','H3O'],products:['NH4','H2O'],donor:1,acceptor:0,name:'reverse proton transfer'}}};
export class ReactionCloseupView{
constructor(root){this.root=root;this.scenario=null;this.state=null;this.cycleOrigin=performance.now();this.frozen=false;this.freezeTime=0;this.equilibriumHoldUntil=0;this.wasEquivalenceFocus=false;this.renderedEquationMode=null;this.renderShell();this.resizeObserver=new ResizeObserver(()=>this.resize());this.resizeObserver.observe(root);this.resize();this.frame=requestAnimationFrame(t=>this.animate(t));}
renderShell(){this.root.innerHTML=`<div class="reaction-equation-stack" id="reaction-equation-closeup"></div><div class="reaction-stage-wrap"><canvas class="reaction-canvas" aria-label="Magnified three-dimensional acid-base reaction animation"></canvas><div class="reaction-stage-label" id="reaction-stage-label">Net ionic reaction</div></div>`;this.stage=this.root.querySelector('.reaction-stage-wrap');this.canvas=this.root.querySelector('canvas');this.ctx=this.canvas.getContext('2d');this.equation=this.root.querySelector('#reaction-equation-closeup');this.label=this.root.querySelector('#reaction-stage-label');}
resize(){const w=Math.max(1,this.stage.clientWidth),h=Math.max(1,this.stage.clientHeight),dpr=Math.min(window.devicePixelRatio||1,2);this.canvas.width=Math.round(w*dpr);this.canvas.height=Math.round(h*dpr);this.canvas.style.width='100%';this.canvas.style.height='100%';this.dpr=dpr;}
setFrozen(frozen){if(frozen&&!this.frozen)this.freezeTime=performance.now();if(!frozen&&this.frozen){const paused=performance.now()-this.freezeTime;this.cycleOrigin+=paused;this.equilibriumHoldUntil+=paused;}this.frozen=Boolean(frozen);}
equivalenceToleranceMl(){const eq=Number(this.scenario?.equivalenceMl||0);return Math.max(.03,eq*.002);}displayedTitrantMl(){const text=document.querySelector('#volume-readout')?.textContent||'';const parsed=Number.parseFloat(text);return Number.isFinite(parsed)?parsed:Number(this.state?.titrantMl||0);}isEquivalenceFocus(ml=this.displayedTitrantMl()){if(!this.scenario||!EQUIVALENCE_EQUILIBRIA[this.scenario.kind])return false;const eq=Number(this.scenario.equivalenceMl||0);return eq>0&&Math.abs(Number(ml)-eq)<=this.equivalenceToleranceMl();}
renderEquations(showEquilibrium){const mode=showEquilibrium?'equilibrium':'neutralization';if(mode===this.renderedEquationMode)return;this.renderedEquationMode=mode;const net=NET_IONIC[this.scenario.kind],eq=EQUIVALENCE_EQUILIBRIA[this.scenario.kind];const equilibriumRow=showEquilibrium&&eq?`<div class="reaction-equation-row active" data-reaction-row="equilibrium"><span>Dynamic equilibrium at equivalence</span><strong>${eq.equation}</strong></div>`:'';this.equation.innerHTML=`<div class="reaction-equation-row ${showEquilibrium?'':'active'}" data-reaction-row="neutralization"><span>Net ionic titration reaction</span><strong>${net.equation}</strong></div>${equilibriumRow}`;}
setState(scenario,state){const changedScenario=this.scenario?.kind!==scenario.kind;this.scenario=scenario;this.state=state;if(changedScenario){this.cycleOrigin=performance.now();this.equilibriumHoldUntil=0;this.wasEquivalenceFocus=false;this.renderedEquationMode=null;}const actualEquivalence=this.isEquivalenceFocus(Number(state?.titrantMl||0));if(actualEquivalence&&!this.wasEquivalenceFocus){this.cycleOrigin=performance.now();this.equilibriumHoldUntil=performance.now()+4600;}this.wasEquivalenceFocus=actualEquivalence;this.renderEquations(actualEquivalence||performance.now()<this.equilibriumHoldUntil);}
trigger(){if(!this.isEquivalenceFocus())this.cycleOrigin=performance.now();}animate(time){this.draw(this.frozen?this.freezeTime:time);this.frame=requestAnimationFrame(t=>this.animate(t));}setActiveEquation(mode){this.root.querySelectorAll('[data-reaction-row]').forEach(row=>row.classList.toggle('active',row.dataset.reactionRow===mode));}
drawReaction(ctx,reaction,time,progress,{equilibrium=false}={}){const w=this.canvas.width/this.dpr,h=this.canvas.height/this.dpr,scale=Math.max(42,Math.min(72,w*.075)),y=h*.50,t=Math.max(0,Math.min(1,progress));const approach=Math.min(1,t/.28),transfer=Math.max(0,Math.min(1,(t-.28)/.28)),separate=Math.max(0,Math.min(1,(t-.56)/.24));const leftRest=w*.28,rightRest=w*.72,leftMeet=w*.435,rightMeet=w*.565,leftX=leftRest+(leftMeet-leftRest)*approach,rightX=rightRest+(rightMeet-rightRest)*approach;const rotA=[time*.00034,time*.00049,time*.00027],rotB=[time*.00041,-time*.00037,time*.00031],reactantAlpha=Math.max(0,1-transfer);drawMolecule3D(ctx,reaction.reactants[0],{x:leftX,y},scale,rotA,-.1,reactantAlpha);drawMolecule3D(ctx,reaction.reactants[1],{x:rightX,y},scale,rotB,.15,reactantAlpha);const products=reaction.products.filter(Boolean);if(products.length===1)drawMolecule3D(ctx,products[0],{x:w*.5,y},scale*1.06,rotA,0,transfer);else{const x1=w*(.5-.18*separate),x2=w*(.5+.18*separate);products.forEach((kind,index)=>{let alpha=transfer;if(kind==='H2O'&&!equilibrium&&t>.70)alpha*=Math.max(.08,1-(t-.70)/.25);drawMolecule3D(ctx,kind,{x:index===0?x1:x2,y},scale,index===0?rotA:rotB,index===0?-.08:.10,alpha);});}if(t>.28&&t<.62){const q=(t-.28)/.34,donorX=reaction.donor===0?leftMeet:rightMeet,acceptorX=reaction.acceptor===0?leftMeet:rightMeet,direction=reaction.donor===0?1:-1,startX=donorX+direction*scale*.25,endX=acceptorX-direction*scale*.25,arcY=y-Math.sin(q*Math.PI)*scale*.50;drawProton(ctx,startX+(endX-startX)*q,arcY,Math.max(8,scale*.14),Math.sin(q*Math.PI));}if(equilibrium){if(t<.28)this.label.textContent=`${reaction.name}: reactants approach.`;else if(t<.62)this.label.textContent=`${reaction.name}: proton transfer.`;else this.label.textContent=`${reaction.name}: products separate; the reverse reaction follows.`;}else{if(t<.28)this.label.textContent='Net ionic reaction: reactants approach.';else if(t<.62)this.label.textContent='Net ionic reaction: proton transfer occurs.';else this.label.textContent='Net ionic reaction: products separate; reaction water fades into the solvent.';}}
draw(time){if(!this.ctx||!this.scenario||!this.state||!this.dpr)return;const ctx=this.ctx,w=this.canvas.width/this.dpr,h=this.canvas.height/this.dpr;ctx.setTransform(this.dpr,0,0,this.dpr,0,0);ctx.clearRect(0,0,w,h);const equilibrium=EQUIVALENCE_EQUILIBRIA[this.scenario.kind],displayedEquivalence=this.isEquivalenceFocus();if(displayedEquivalence&&!this.wasEquivalenceFocus){this.cycleOrigin=time;this.equilibriumHoldUntil=time+4600;}if(displayedEquivalence)this.wasEquivalenceFocus=true;const showEquilibrium=Boolean(equilibrium)&&(displayedEquivalence||time<this.equilibriumHoldUntil);this.renderEquations(showEquilibrium);if(showEquilibrium){this.setActiveEquation('equilibrium');const cycleMs=3600,elapsed=Math.max(0,time-this.cycleOrigin),cycle=Math.floor(elapsed/cycleMs),progress=(elapsed%cycleMs)/cycleMs,reaction=cycle%2===0?equilibrium.forward:equilibrium.reverse;this.drawReaction(ctx,reaction,time,progress,{equilibrium:true});return;}if(!displayedEquivalence)this.wasEquivalenceFocus=false;this.setActiveEquation('neutralization');const cycleMs=4000,elapsed=Math.max(0,time-this.cycleOrigin),progress=(elapsed%cycleMs)/cycleMs;this.drawReaction(ctx,NET_IONIC[this.scenario.kind],time,progress);}
}
